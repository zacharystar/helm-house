apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helm-house.fullname" . }}-test-connection"
  labels:
    {{- include "helm-house.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helm-house.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
