apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-house.fullname" . }}
  labels:
    {{- include "helm-house.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-house.selectorLabels" . | nindent 4 }}
